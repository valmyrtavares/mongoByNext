/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: {
  //   appDir: true,
  // },
  images: {
    domains: ['i.pinimg.com'], // adicione o domínio da imagem aqui
  },
};

module.exports = nextConfig;
